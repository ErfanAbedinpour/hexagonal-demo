# Development Docker Compose for Hexagonal Architecture App

# - Hot reload enabled

# - Uses memory cache and mock providers

# - Debug logging

#

# Usage:

# docker compose -f deployment/dev/docker-compose.yml up --build

services:
  app:
    build:
      context: ../..
      dockerfile: deployment/Dockerfile
      target: development
    env_file:
      - ./.env
    ports:
      - '3000:3000'
    volumes:
      - ../..:/app
    command: npm run start:dev
    depends_on: []
